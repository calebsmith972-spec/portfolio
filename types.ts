
/**
 * Interface representing a project in the portfolio.
 */
export interface Project {
  id: string;
  name: string;
  logo: string;
  url: string;
}

/**
 * Interface representing a professional capability or skill.
 */
export interface Capability {
  title: string;
  description: string;
}
